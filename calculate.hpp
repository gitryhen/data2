#include <vector>
#include <string>

template<typename T>
T stdev(const std::vector<T>&);
template<typename T>
std::vector<T> collectMaxInSections(const std::vector<T> t);
std::string calcBaseLine( const std::vector<std::vector<std::string> > & dat, const unsigned colnbr);
